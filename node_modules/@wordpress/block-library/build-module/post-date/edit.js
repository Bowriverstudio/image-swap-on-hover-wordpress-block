import _slicedToArray from "@babel/runtime/helpers/esm/slicedToArray";
import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { useEntityProp, useEntityId } from '@wordpress/core-data';
import { __experimentalGetSettings, dateI18n } from '@wordpress/date';
import { __ } from '@wordpress/i18n';

function PostDateDisplay() {
  var _useEntityProp = useEntityProp('postType', 'post', 'date'),
      _useEntityProp2 = _slicedToArray(_useEntityProp, 1),
      date = _useEntityProp2[0];

  var settings = __experimentalGetSettings();

  return date ? createElement("time", {
    dateTime: dateI18n('c', date)
  }, dateI18n(settings.formats.date, date)) : __('No Date');
}

export default function PostDateEdit() {
  if (!useEntityId('postType', 'post')) {
    return 'Post Date Placeholder';
  }

  return createElement(PostDateDisplay, null);
}
//# sourceMappingURL=edit.js.map