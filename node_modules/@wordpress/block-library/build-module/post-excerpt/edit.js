import _slicedToArray from "@babel/runtime/helpers/esm/slicedToArray";
import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { useEntityProp, useEntityId } from '@wordpress/core-data';
import { PlainText } from '@wordpress/block-editor';

function PostExcerptDisplay() {
  var _useEntityProp = useEntityProp('postType', 'post', 'excerpt'),
      _useEntityProp2 = _slicedToArray(_useEntityProp, 2),
      excerpt = _useEntityProp2[0],
      setExcerpt = _useEntityProp2[1];

  return createElement(PlainText, {
    value: excerpt,
    onChange: setExcerpt
  });
}

export default function PostExcerptEdit() {
  if (!useEntityId('postType', 'post')) {
    return 'Post Excerpt Placeholder';
  }

  return createElement(PostExcerptDisplay, null);
}
//# sourceMappingURL=edit.js.map