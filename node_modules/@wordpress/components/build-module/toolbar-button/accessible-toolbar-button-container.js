import { createElement } from "@wordpress/element";

/**
 * External dependencies
 */
import { useToolbarItem } from 'reakit/Toolbar';
/**
 * WordPress dependencies
 */

import { Children, cloneElement, useContext } from '@wordpress/element';
/**
 * Internal dependencies
 */

import ToolbarContext from '../toolbar-context';

function AccessibleToolbarButtonContainer(props) {
  var accessibleToolbarState = useContext(ToolbarContext);
  var childButton = Children.only(props.children); // https://reakit.io/docs/composition/#props-hooks

  var itemHTMLProps = useToolbarItem(accessibleToolbarState, childButton.props);
  return createElement("div", props, cloneElement(childButton, itemHTMLProps));
}

export default AccessibleToolbarButtonContainer;
//# sourceMappingURL=accessible-toolbar-button-container.js.map